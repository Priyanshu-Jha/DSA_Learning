#include<iostream>
#include<vector>
using namespace std;

vector<string> restoreIpAddresses(string s) {
        
}

int main(){
    string str;
    cin>>str;
    vector<string> ans = restoreIpAddresses(str);
    for(auto x:ans)
        cout<<x<<" ";
}